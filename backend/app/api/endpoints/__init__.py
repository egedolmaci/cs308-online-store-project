# Enables "from app.api.endpoints import auth"
# Explicit imports for routers
from app.api.endpoints import (
    auth,
    products,
    orders,
    categories,
    reviews,
    support,
    users,
    wishlist,
)
